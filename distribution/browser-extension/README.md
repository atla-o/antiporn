# Browser extension channel

The unpacked extension is `/extension` at the repo root.

- Manifest V3
- Content script squares over `img` / `video`
- Same high-recall detector as the web app
- Zip for drag-drop: `npm run pack:extension` → `public/downloads/antiporn-extension.zip`

Sideload docs: `distribution/drag-drop/README.md`.
